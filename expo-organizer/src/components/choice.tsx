import { Pressable, StyleSheet, Text, View } from "react-native";
import { colors } from "@/lib/theme";
export function Choice({ value, onChange, items }: { value: string; onChange: (value: string) => void; items: { value: string; label: string }[] }) { return <View style={styles.wrap}>{items.map((item) => <Pressable key={item.value} accessibilityRole="button" accessibilityState={{ selected: value === item.value }} onPress={() => onChange(item.value)} style={[styles.item, value === item.value && styles.active]}><Text style={[styles.text, value === item.value && styles.activeText]}>{item.label}</Text></Pressable>)}</View>; }
const styles = StyleSheet.create({ wrap: { flexDirection: "row", flexWrap: "wrap", gap: 8 }, item: { minHeight: 44, justifyContent: "center", paddingHorizontal: 13, borderRadius: 8, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface }, active: { borderColor: colors.primary, backgroundColor: "#382311" }, text: { color: colors.muted, fontSize: 14, fontWeight: "700" }, activeText: { color: colors.text } });
