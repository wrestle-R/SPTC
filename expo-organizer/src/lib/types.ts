import type { CricketInningsState, Player, Team } from "@sports-fiesta/domain";
export type { Player, Team };
export interface Match { id: string; sport: "football" | "handball" | "cricket"; stage: "league" | "final"; status: "scheduled" | "lineup" | "live" | "innings-break" | "super-over" | "completed"; homeTeamId: string; awayTeamId: string; startsAt: string; venue: string; revision: number; lineups: Record<string, { starters: string[]; substitutes: string[] }>; scoreSummary: Record<string, number> & { innings?: { battingTeamId: string; score: number; wickets: number; overs: string }[] }; fieldState?: { score: Record<string, number>; shootout: Record<string, number>; events: Record<string, string | number>[] }; cricket?: { innings: { state: CricketInningsState; initial: Record<string, unknown>; superOver?: boolean }[]; currentInnings: number }; resultText?: string; }
